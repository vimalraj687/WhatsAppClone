import React from "react";

const EmptyChat = () => {
  return (
    <h1>
      Open Git Bash: Launch the Git Bash terminal on your computer. If you don't
      have Git Bash installed, you can download and install it from the official
      Git website. Navigate to the desired directory: Use the cd command to
      navigate to the directory where you want to create your Git repository.
      For example, to navigate to the "Documents" folder, you can use the
      following command:
    </h1>
  );
};

export default EmptyChat;
